package sieger;

import org.springframework.boot.SpringApplication;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The main class that the JVM will run.
 * @author Irvan Sian Syah Putra
 *
 */
@SpringBootApplication
public class SiegerApplication {

	/**
	 * The main method, the method that gets called first when the app starts.
	 * @param args
	 */
	public static void main(String[] args) {
		SpringApplication.run(SiegerApplication.class, args);
		
	}

}
